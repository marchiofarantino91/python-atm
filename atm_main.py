import random
import datetime
from customer import Customer

atm  = Customer(id)

while True:
     id = int(input("masukan pin anda:"))

     trial=0
     
while (id != int(atm.getCustPin()) and trial < ):
    id = int(input("masukan pin anda:")) 
    trial=0